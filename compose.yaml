services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: athena
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
